// This file has been intentionally left empty.
// All mock data has been removed — the application is now fully wired to the real API.
// mockRooms → GET /api/labs
// mockBookings → GET /api/bookings (JWT-scoped)
// mockROIReport → GET /api/bookings/:id
export {};
